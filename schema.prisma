
datasource db {
  provider = "sqlite"
  url      = "file:database.db"
}

generator client {
  provider = "prisma-client-py"
}

// data model
model Item {
  sku        String @id
  name       String
  description String
  color      String?
  size       String?
  count      Int
}
